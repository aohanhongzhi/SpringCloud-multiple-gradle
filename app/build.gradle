archivesBaseName = 'app'

jar.enabled = true
bootJar.enabled = true


dependencies {
    compile project(':common') /* 子模块之间的依赖 */
    implementation 'org.springframework.boot:spring-boot-starter-mail'

    testCompile group: 'junit', name: 'junit', version: '4.12'
}
